// ----------------------------------------------------------------------

import { DashboardView } from '../sections/Shopee/dashboard/view';

export const metadata = {
  title: 'Minimal: The starting point for your next project',
};

export default function HomePage() {
  return <DashboardView />;
}
